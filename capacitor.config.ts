import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'logicseve_V1_Sh',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
